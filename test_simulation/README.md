# 🧪 Симуляция тестов

Эта папка содержит реальные тесты для проверки работы системы мониторинга.

## 📦 Установка зависимостей

```bash
pip install requests pytest pytest-json-report
```

## 🚀 Запуск тестов

### Вариант 1: Все тесты
```bash
pytest test_simulation/ --json-report --json-report-file=test_reports/report.json -v
```

### Вариант 2: Только API тесты
```bash
pytest test_simulation/test_api.py --json-report --json-report-file=test_reports/report.json -v
```

### Вариант 3: Только веб-тесты
```bash
pytest test_simulation/test_website.py --json-report --json-report-file=test_reports/report.json -v
```

## 📊 Что тестируется

### test_api.py
- ✅ Успешные запросы к JSONPlaceholder API
- ❌ Ошибки 404 (не найдено)
- ❌ Timeout ошибки
- ❌ Connection refused
- ❌ Assertion failures
- ❌ Unauthorized (401)
- ❌ Server errors (500)

### test_website.py
- ✅ Проверка доступности сайтов (Google, GitHub)
- ❌ HTTP статусы (404, 500)
- ❌ Timeout на медленных запросах
- ❌ Ошибки подключения
- ❌ Неверные ожидания в JSON

## 🎯 Ожидаемый результат

После запуска тестов:
1. Создастся файл `test_reports/report.json`
2. Система мониторинга обнаружит новый отчет
3. В Telegram придут уведомления с анализом каждого упавшего теста
4. Для каждой ошибки будут даны конкретные рекомендации

## 💡 Примеры уведомлений

**Успешный тест:**
```
✅ Тест прошел успешно

📝 test_get_users_success
```

**Упавший тест:**
```
🟡 РАЗОВАЯ ОШИБКА

❌ Упал тест: test_timeout_error

🔍 Диагноз: Превышено время ожидания ответа

📋 Что проверить:
1. Проверь нагрузку на сервер
2. Убедись, что сервис доступен
3. Проверь стабильность сети
4. Увеличь таймаут в конфигурации теста
```

## 🔄 Полный цикл проверки

1. Запусти мониторинг:
```bash
python watcher.py
```

2. В другом терминале запусти тесты:
```bash
pytest test_simulation/ --json-report --json-report-file=test_reports/report.json -v
```

3. Проверь Telegram - должны прийти уведомления
4. Используй кнопки в боте для просмотра статистики
