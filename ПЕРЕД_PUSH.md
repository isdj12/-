# 📦 Чеклист перед Git Push

## ✅ Что сделано

1. ✓ `.gitignore` обновлен
2. ✓ `config.py` удален из git (содержит токены!)
3. ✓ Старые папки удалены (my_tests, test_website, user_tests)
4. ✓ База данных исключена из git
5. ✓ Отчеты исключены из git

## 🔍 Проверка перед push

Выполни в WSL:

```bash
cd ~/ai-mozg

# Проверь статус
git status

# Убедись что config.py НЕ в списке!
# Должно быть: "D  config.py" (deleted)
```

## 📝 Что будет в коммите

### Изменено (Modified):
- `.gitignore` - обновлены правила
- `ai_brain.py` - добавлен выбор проекта
- `core.py` - настроен Ollama
- `config.example.py` - пример конфига
- `README.md` - обновлена документация

### Удалено (Deleted):
- `config.py` - содержит токены!
- `my_tests/` - старая папка
- `test_website/` - старая папка
- `user_tests/` - старая папка

### Добавлено (Untracked):
- `tests/` - новая папка для тестов
- `check_setup.py` - проверка настройки
- `setup_linux.sh` - скрипт установки
- `INSTALL_WSL.md` - инструкция WSL
- `GIT_READY.md` - эта инструкция

## 🚀 Команды для push

```bash
cd ~/ai-mozg

# Добавить все изменения
git add .

# Проверить что добавлено (config.py НЕ должен быть!)
git status

# Сделать коммит
git commit -m "Refactor: Setup AI-МОЗГ with Ollama, add project selection"

# Запушить
git push origin main
```

## ⚠️ ВАЖНО!

### НЕ должны попасть в git:
- ❌ `config.py` (токены!)
- ❌ `test_history.db` (база данных)
- ❌ `test_reports/` (отчеты)
- ❌ `__pycache__/` (кэш Python)
- ❌ `.pytest_cache/` (кэш pytest)

### Должны попасть в git:
- ✓ `config.example.py` (пример)
- ✓ `ai_brain.py` (код)
- ✓ `core.py` (код)
- ✓ `tests/` (примеры тестов)
- ✓ `.gitignore` (правила)
- ✓ `README.md` (документация)

## 🔒 После клонирования

На новой машине нужно:

```bash
# Клонировать
git clone <твой-репозиторий>
cd ai-mozg

# Создать config.py
cp config.example.py config.py

# Отредактировать config.py
nano config.py
# Добавить BOT_TOKEN и CHAT_ID

# Установить зависимости
pip install -r requirements.txt

# Запустить Ollama
ollama serve

# Запустить AI-мозг
python3 ai_brain.py
```

## ✅ Готово к push!

Если все проверки пройдены - можешь делать push!

```bash
git add .
git commit -m "Setup AI-МОЗГ with Ollama"
git push
```
