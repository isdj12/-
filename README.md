# 🧠 AI-МОЗГ для автоматизации тестирования

Умный ассистент для запуска тестов через Telegram с AI-анализом ошибок на базе Ollama.

## 🚀 Возможности

- 🤖 Управление через Telegram бота
- 🧪 Автоматический запуск pytest тестов
- 🔍 AI-анализ ошибок через локальную модель Ollama (llama3.2)
- 📊 Статистика и история тестов
- 💡 Конкретные рекомендации по исправлению
- 🆓 Полностью бесплатно и работает офлайн

## ⚡ Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка

Скопируй `config.example.py` в `config.py` и заполни:

```python
BOT_TOKEN = 'твой_токен_от_BotFather'  # Получи у @BotFather
CHAT_ID = 'твой_chat_id'                # Получи у @userinfobot
```

### 3. Запуск Ollama

```bash
ollama serve
```

### 4. Запуск AI-МОЗГА

```bash
python3 ai_brain.py
```

При запуске выбери проект:
- Текущая директория (ai-mozg)
- Другой проект (укажи путь)
- Родительская директория

## 📁 Структура проекта

```
ai-mozg/
├── ai_brain.py          # Главный файл
├── core.py              # AI-анализатор и парсер
├── config.py            # Настройки (не в git)
├── tests/               # Твои тесты
│   └── test_example.py
├── test_reports/        # Отчеты (не в git)
└── test_history.db      # БД (не в git)
```

## 🧪 Создание тестов

Создавай файлы `test_*.py` в папке `tests/`:

```python
import requests

def test_my_website():
    """Проверка доступности сайта"""
    response = requests.get("https://example.com")
    assert response.status_code == 200

def test_api_response():
    """Проверка API"""
    response = requests.get("https://api.example.com/data")
    assert response.status_code == 200
    data = response.json()
    assert 'result' in data
```

## 📱 Команды в Telegram

После запуска отправь боту `/start` и используй кнопки:

- **🚀 Запустить тесты** - запуск всех тестов с AI-анализом
- **📊 Последний отчет** - статистика за 24 часа
- **🔥 Топ проблем** - самые падающие тесты за неделю
- **🤖 AI анализ** - глубокий анализ последних ошибок
- **📈 Статистика** - общая статистика по всем тестам
- **🗑️ Очистить БД** - удалить всю историю

## 🤖 AI-анализ с Ollama

AI-МОЗГ использует локальную модель **llama3.2** через Ollama:

- ✓ Полностью бесплатно
- ✓ Работает офлайн
- ✓ Быстрый анализ ошибок
- ✓ Конкретные рекомендации на русском языке
- ✓ Понимает контекст ошибки

### Установка Ollama

```bash
# Установка (если еще не установлен)
curl -fsSL https://ollama.com/install.sh | sh

# Загрузка модели
ollama pull llama3.2

# Запуск
ollama serve
```

## 🎯 Как это работает

1. Запускаешь `python3 ai_brain.py`
2. Выбираешь проект с тестами
3. Открываешь Telegram и жмешь **🚀 Запустить тесты**
4. AI-МОЗГ:
   - Запускает pytest в папке tests/
   - Собирает результаты
   - Анализирует ошибки через Ollama
   - Отправляет умный отчет с рекомендациями

## 📊 Пример отчета

```
[ВНИМАНИЕ]

Тест: test_website_status

🤖 AI Диагноз:
Сервер вернул код 404 вместо ожидаемого 200

Рекомендации:
1. Проверь правильность URL
2. Убедись что ресурс существует
3. Проверь роутинг приложения
4. Проверь миграции базы данных

⚠️ Тест падал 3 раза за последние 7 дней
```

## 🔧 Требования

- Python 3.8+
- Ollama с моделью llama3.2
- Telegram бот (токен от @BotFather)
- pytest и pytest-json-report

## 📝 Git

Проект готов к push в git:

```bash
git add .
git commit -m "Initial commit"
git push
```

Файлы `config.py`, `*.db` и `test_reports/` автоматически исключены через `.gitignore`

## 🆚 Преимущества Ollama

| Параметр | Ollama | OpenAI/Gemini |
|----------|--------|---------------|
| Стоимость | Бесплатно | Платно |
| Интернет | Не нужен | Обязателен |
| Приватность | 100% | Данные уходят в облако |
| Скорость | Быстро | Зависит от сети |

## 📖 Документация

- `START_HERE.txt` - быстрый старт
- `SETUP.md` - детальная настройка
- `ЗАПУСК.md` - инструкция по запуску
- `tests/README.md` - как писать тесты

## 🐛 Проблемы?

**Ollama не отвечает:**
```bash
ollama serve
```

**pytest не найден:**
```bash
pip install pytest pytest-json-report
```

**Бот не реагирует:**
- Проверь BOT_TOKEN в config.py
- Убедись что ai_brain.py запущен
- Отправь боту /start

## 📄 Лицензия

MIT License

---

Сделано с ❤️ для автоматизации тестирования
