# ✅ Проект готов к Git

## 📦 Что будет загружено в Git

### ✓ Основные файлы:
- `ai_brain.py` - главный файл
- `core.py` - AI-анализатор
- `config.example.py` - пример конфигурации
- `requirements.txt` - зависимости
- `.gitignore` - правила исключения

### ✓ Документация:
- `README.md` - основная документация
- `START_HERE.txt` - быстрый старт
- `INSTALL_WSL.md` - установка в WSL
- `LICENSE` - лицензия

### ✓ Тесты:
- `tests/` - папка с примерами тестов
- `check_setup.py` - скрипт проверки
- `setup_linux.sh` - скрипт установки

## ❌ Что НЕ попадет в Git

### Конфигурация (содержит токены!):
- `config.py` ❌
- `monitor_config.json` ❌

### База данных:
- `test_history.db` ❌
- `*.db`, `*.sqlite` ❌

### Отчеты:
- `test_reports/` ❌
- `*.json` ❌

### Python кэш:
- `__pycache__/` ❌
- `*.pyc` ❌
- `.pytest_cache/` ❌

### Временные файлы:
- `ГОТОВО.md` ❌
- `ЗАПУСК.md` ❌
- `SETUP.md` ❌

## 🚀 Команды для Git

### Первый коммит:

```bash
cd ~/ai-mozg

# Добавить все файлы
git add .

# Проверить что добавлено
git status

# Сделать коммит
git commit -m "Setup AI-МОЗГ with Ollama support"

# Запушить
git push
```

### Проверка перед коммитом:

```bash
# Посмотреть что будет добавлено
git status

# Посмотреть изменения
git diff

# Убедиться что config.py НЕ в списке
git status | grep config.py
# Должно быть пусто!
```

## ⚠️ Важно!

После клонирования репозитория на другой машине:

1. Скопируй `config.example.py` в `config.py`
2. Заполни `BOT_TOKEN` и `CHAT_ID`
3. Установи зависимости: `pip install -r requirements.txt`
4. Запусти Ollama: `ollama serve`
5. Запусти AI-мозг: `python3 ai_brain.py`

## 🔒 Безопасность

`.gitignore` настроен так, чтобы:
- ✓ Токены и ключи НЕ попали в git
- ✓ База данных осталась локальной
- ✓ Отчеты не загружались
- ✓ Кэш Python исключен

**Проверь перед push что config.py не в списке файлов!**
